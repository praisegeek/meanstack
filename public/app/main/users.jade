#users
    .jumbotron.page
        .container
            .page-header
                h1 USERS
                small {{menu}}
                
            ul.list-group(ng-repeat="user in users")
                .list-group-item {{user.firstName}} {{user.lastName}}
                    .badge {{user.roles.toString() }}